$schema: "https://json-schema.org/draft/2020-12/schema"
$id: "https://computable-lab.com/schema/computable-lab/equipment-class.schema.yaml"
title: "Equipment Class"
description: >
  Reusable metadata for a class of equipment. Semantic verb support stays in
  equipment-capability records, but class metadata can declare expected readiness
  requirements and backend hints.

type: object
unevaluatedProperties: false
allOf:
- $ref: "./common.schema.yaml#/$defs/FAIRCommon"

required:
- kind
- id
- name

properties:
  kind:
    const: "equipment-class"

  id:
    type: string
    pattern: "^EQC-[A-Z0-9][A-Z0-9_-]*$"

  name:
    type: string
    minLength: 1

  manufacturer:
    type: string

  modelFamily:
    type: string

  executionBackends:
    type: array
    items: { type: string }
    uniqueItems: true

  readinessRequirements:
    type: object
    additionalProperties: false
    properties:
      calibrationRequired:
        type: boolean
      qualificationRequired:
        type: boolean

  # First-class equipment capability (declarative): the PRODUCED-parameters this
  # class exposes (e.g. a water bath's temperature_c), and the LABWARE it accepts
  # (e.g. a qPCR machine accepts a plate_96). Instances set concrete `settings`
  # keyed by these definitions; only `acceptsLabware` instruments may receive
  # labware placed onto them. Empty/absent acceptsLabware = accepts none.
  settingsDefinition:
    type: array
    description: >-
      Configurable parameters this class exposes. Each is a key the instance's
      `settings` must/would set. Declarative: a water-bath class declares
      `temperature_c`; a thermocycler declares `anneal_temperature_c`/`cycles`.
    uniqueItems: true
    items:
      type: object
      additionalProperties: false
      required: [ key, label ]
      properties:
        key: { type: string, minLength: 1 }
        label: { type: string, minLength: 1 }
        valueType: { type: string, enum: [ number, string, boolean, enum, duration_sec, profile ] }
        unit: { type: string }
        min: { type: number }
        max: { type: number }
        enum: { type: array, items: { type: string } }
        # Declarative shape for a multi-step cycling program (valueType: profile).
        # A thermocycler declares its run profile here; the instance sets the
        # concrete value under the same key in `settings`. Nothing here is labware
        # geometry — it is the machine's run program.
        profileDefinition:
          type: object
          additionalProperties: false
          required: [ initial, cycles ]
          properties:
            initial:
              type: object
              additionalProperties: false
              required: [ temperature_c, duration_sec ]
              properties:
                temperature_c: { type: number }
                duration_sec: { type: number }
            cycles:
              type: object
              additionalProperties: false
              required: [ count, steps ]
              properties:
                count: { type: integer, exclusiveMinimum: 0 }
                steps:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    additionalProperties: false
                    required: [ temperature_c, duration_sec ]
                    properties:
                      temperature_c: { type: number }
                      duration_sec: { type: number }

  acceptsLabware:
    type: array
    description: >-
      Labware types this equipment accepts, e.g. qPCR accepts `plate_96`. Only
      instruments that declare a type here may have that labware placed onto
      them. Empty/absent = accepts no labware.
    uniqueItems: true
    items: { type: string }

  notes:
    type: string

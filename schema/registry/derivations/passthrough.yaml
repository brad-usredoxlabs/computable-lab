$schema: https://computable-lab.com/schema/computable-lab/registry/derivation.schema.yaml
kind: derivation
id: DRV-PASSTHROUGH
canonical: passthrough
description: Identity function — returns the field value as-is. Used for simple string-typed identity components.
inputType: string
returnType: string

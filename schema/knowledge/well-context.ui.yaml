# UI specification for WellContext records
uiVersion: 1
schemaId: "https://computable-lab.com/schema/computable-lab/well-context.schema.yaml"

display:
  titleField: "$.title"
  icon: "well-context"

form:
  layout: "sections"
  sections:
  - title: "Identity"
    fields:
    - path: "$.recordId"
      widget: "text"
      label: "Record ID"
      readonly: true
    - path: "$.kind"
      widget: "text"
      label: "Kind"
      readonly: true
      hidden: true
  - title: "Well Context"
    fields:
    - path: "$.title"
      widget: "text"
      label: "Title"
    - path: "$.content"
      widget: "markdown"
      label: "Content"

list:
  columns:
  - path: "$.recordId"
    label: "ID"
    width: "120px"
  - path: "$.title"
    label: "Title"

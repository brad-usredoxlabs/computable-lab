kind: protocol-foundry-architect-verdict
protocolId: mp10422
variant: bench_plate_multichannel
generated_at: 2026-05-09T22:42:10.330Z
accepted: false
qualityScore: 0.05
coverageEstimate: 0
failureClasses:
  - precompiler_reference_shape
  - extractor_prompt_contract
missingVerbs: []
missingLabware: []
missingMaterials: []
badEvents: []
badScalingAssumptions: []
recommendedFixes:
  - id: fix-ai-precompile-fallback-shape
    class: precompiler_reference_shape
    title: Fix ai_precompile fallback to produce non-empty candidateEvents when
      tag_prompt fails
    rationale: The compiler diagnostics show 'ai_precompile output shape mismatch,
      using fallback' followed by all downstream passes being skipped because
      outputs.ai_precompile.candidateEvents evaluated to false. The tag_prompt
      pass failed with a JSON parse error (Unexpected token 'B'), and the
      fallback path in createAiPrecompilePass produces an empty candidateEvents
      array. This causes eventCount=0. The fix must ensure the fallback path
      either retries the LLM call with a stricter JSON schema or produces a
      minimal valid candidateEvents structure from the presegmented candidates
      (9+4+3+6+4+1=27 candidates were extracted).
    ownedFiles:
      - server/src/compiler/pipeline/passes/ChatbotCompilePasses.ts
    acceptance:
      - When tag_prompt returns invalid JSON, the ai_precompile pass must not
        produce an empty candidateEvents array if presegmented candidates exist.
      - The fallback path must map presegmented candidates to candidateEvents
        with at least basic verb/subject/object structure.
      - eventCount must be >= 3 for protocols with clear procedural text.
      - The 'ai_precompile_shape_mismatch' warning should be accompanied by a
        recovery attempt, not silent fallback to empty.
    coderModelProfile:
      model: Kbenkhaled/Qwen3.5-35B-3A-NVFP4
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: &a1
      compiler: /home/brad/git/computable-foundry/artifacts/compiler/mp10422/bench_plate_multichannel.yaml
      eventGraph: /home/brad/git/computable-foundry/artifacts/event-graphs/mp10422/bench_plate_multichannel.yaml
      executionScale: /home/brad/git/computable-foundry/artifacts/execution-scale/mp10422/bench_plate_multichannel.yaml
      browserReport: /home/brad/git/computable-foundry/artifacts/browser-review/mp10422/bench_plate_multichannel/report.yaml
      coderPatch: /home/brad/git/computable-foundry/artifacts/code-patches/mp10422/bench_plate_multichannel/result.yaml
      assumptions: /home/brad/git/computable-foundry/artifacts/assumptions/mp10422/bench_plate_multichannel.yaml
      segment: /home/brad/git/computable-foundry/artifacts/segments/mp10422.yaml
      materialContext: /home/brad/git/computable-foundry/artifacts/material-context/mp10422.yaml
      text: /home/brad/git/computable-foundry/artifacts/text/mp10422.txt
    failureEvidence: &a2
      eventCount: 0
      outcome: gap
    implementationBudget:
      targetChangedFiles: 1
      maxChangedFiles: 2
      targetChangedLines: 60
      maxChangedLines: 150
      requireFocusedFixture: true
  - id: fix-tag-prompt-json-schema
    class: extractor_prompt_contract
    title: Enforce strict JSON output from tag_prompt LLM call to prevent parse
      failures
    rationale: "The tag_prompt pass failed because the LLM returned markdown-wrapped
      JSON ('```json\\n{...}') instead of raw JSON. The parser encountered
      'Based on the provided text...' preamble text before the JSON block. The
      fix should either: (1) add a response format constraint to the LLM prompt
      requesting raw JSON only, (2) add a post-processing step to extract JSON
      from markdown code blocks, or (3) use a JSON schema validation library to
      parse and extract the JSON object from the response. This is a root cause
      of the ai_precompile shape mismatch."
    ownedFiles:
      - server/src/compiler/pipeline/passes/ChatbotCompilePasses.ts
      - server/src/extract/ExtractionRunnerService.ts
    acceptance:
      - tag_prompt responses wrapped in markdown code blocks must be correctly
        parsed.
      - tag_prompt responses with preamble text must have the JSON object
        extracted.
      - The tag_prompt pass should not produce 'tag_prompt_parse_error' warnings
        for valid JSON responses wrapped in markdown.
      - Integration test with a sample LLM response containing markdown wrapping
        should pass.
    coderModelProfile:
      model: Kbenkhaled/Qwen3.5-35B-3A-NVFP4
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: *a1
    failureEvidence: *a2
    implementationBudget:
      targetChangedFiles: 1
      maxChangedFiles: 3
      targetChangedLines: 40
      maxChangedLines: 100
      requireFocusedFixture: true
sourceArtifacts: *a1
architectNotes: "LLM architect analysis: 2 fix recommendations."

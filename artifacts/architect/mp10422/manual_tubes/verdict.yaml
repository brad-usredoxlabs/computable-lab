kind: protocol-foundry-architect-verdict
protocolId: mp10422
variant: manual_tubes
generated_at: 2026-05-09T22:41:21.899Z
accepted: false
qualityScore: 0.05
coverageEstimate: 0
failureClasses:
  - precompiler_reference_shape
  - extractor_prompt_contract
missingVerbs: []
missingLabware: []
missingMaterials: []
badEvents: []
badScalingAssumptions: []
recommendedFixes:
  - id: fix-ai-precompile-shape-mismatch-and-fallback
    class: precompiler_reference_shape
    title: Fix ai_precompile shape mismatch and ensure candidateEvents are populated
      from presegmented candidates
    rationale: The compiler diagnostics show 'ai_precompile output shape mismatch,
      using fallback' and subsequently all downstream passes
      (expand_biology_verbs, resolve_labware, mint_materials) are skipped
      because outputs.ai_precompile.candidateEvents evaluated to false. The
      pre-compiler supplied 26 total deterministic candidates across chunks
      (8+4+3+6+4+1), but the ai_precompile pass failed to shape them correctly,
      causing the fallback to produce an empty candidateEvents array. This
      results in eventCount=0 despite clear protocol content. The prior patch
      touched ChatbotCompilePasses.ts but the issue persists, indicating the
      shape validation or fallback logic still doesn't properly recover
      candidates from presegmented text.
    ownedFiles:
      - server/src/compiler/pipeline/passes/ChatbotCompilePasses.ts
    acceptance:
      - ai_precompile pass must not emit 'ai_precompile_shape_mismatch' warning
        when presegmented candidates are present
      - outputs.ai_precompile.candidateEvents must be a non-empty array when
        presegmented candidates exist
      - Downstream passes (expand_biology_verbs, resolve_labware) must not be
        skipped when candidateEvents are populated
      - eventCount must be >= 3 for protocols with clear procedural steps
    coderModelProfile:
      model: Qwen/Qwen3.6-27B-FP8
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: &a1
      compiler: /home/brad/git/computable-foundry/artifacts/compiler/mp10422/manual_tubes.yaml
      eventGraph: /home/brad/git/computable-foundry/artifacts/event-graphs/mp10422/manual_tubes.yaml
      executionScale: /home/brad/git/computable-foundry/artifacts/execution-scale/mp10422/manual_tubes.yaml
      browserReport: /home/brad/git/computable-foundry/artifacts/browser-review/mp10422/manual_tubes/report.yaml
      coderPatch: /home/brad/git/computable-foundry/artifacts/code-patches/mp10422/manual_tubes/result.yaml
      assumptions: /home/brad/git/computable-foundry/artifacts/assumptions/mp10422/manual_tubes.yaml
      segment: /home/brad/git/computable-foundry/artifacts/segments/mp10422.yaml
      materialContext: /home/brad/git/computable-foundry/artifacts/material-context/mp10422.yaml
      text: /home/brad/git/computable-foundry/artifacts/text/mp10422.txt
    failureEvidence: &a2
      eventCount: 0
      outcome: gap
    implementationBudget:
      targetChangedFiles: 1
      maxChangedFiles: 2
      targetChangedLines: 120
      maxChangedLines: 250
      requireFocusedFixture: true
  - id: fix-tag-prompt-json-parse-error
    class: extractor_prompt_contract
    title: Fix tag_prompt JSON parse error from unterminated string
    rationale: "The diagnostics show 'tag_prompt response was not valid JSON:
      Unterminated string in JSON at position 741' with a raw_preview showing
      the JSON was cut off mid-string ('CellRO'). This indicates the LLM
      response for tag_prompt is being truncated, likely due to output token
      limits or improper response handling. This parse failure may contribute to
      the ai_precompile shape mismatch if tag_prompt output is consumed by
      ai_precompile. The fix should add robust JSON parsing with truncation
      recovery or increase the response buffer for tag_prompt."
    ownedFiles:
      - server/src/compiler/pipeline/passes/ChatbotCompilePasses.ts
    acceptance:
      - tag_prompt pass must not emit 'tag_prompt_parse_error' warnings
      - JSON responses from tag_prompt must be fully parsed even when LLM output
        is truncated
      - taggedPhrases must be extracted from partial JSON responses using
        recovery parsing
    coderModelProfile:
      model: Kbenkhaled/Qwen3.5-35B-3A-NVFP4
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: *a1
    failureEvidence: *a2
    implementationBudget:
      targetChangedFiles: 1
      maxChangedFiles: 2
      targetChangedLines: 60
      maxChangedLines: 150
      requireFocusedFixture: true
sourceArtifacts: *a1
architectNotes: "LLM architect analysis: 2 fix recommendations."

kind: protocol-foundry-architect-verdict
protocolId: cellrox-oxidative-stress-qr
variant: robot_deck
generated_at: 2026-05-09T22:17:11.648Z
accepted: false
qualityScore: 0.05
coverageEstimate: 0
failureClasses:
  - precompiler_reference_shape
  - compiler_event_coverage
missingVerbs: []
missingLabware: []
missingMaterials: []
badEvents: []
badScalingAssumptions: []
recommendedFixes:
  - id: fix-ai-precompile-json-parse-and-fallback
    class: precompiler_reference_shape
    title: Fix ai_precompile JSON parse error and ensure fallback produces
      candidateEvents
    rationale: "The compiler diagnostics show 'tag_prompt response was not valid
      JSON: Unterminated string in JSON at position 661' followed by
      'ai_precompile output shape mismatch, using fallback'. The fallback path
      produced zero candidateEvents, causing all downstream passes
      (expand_biology_verbs, resolve_labware, mint_materials) to be skipped. The
      root cause is that the ai_precompile pass's fallback does not produce a
      valid AiPrecompileOutput with candidateEvents when the LLM response is
      malformed. The PDF has 8 clear protocol steps that should be extracted.
      The fix must make the fallback path produce at least a minimal set of
      candidateEvents from the deterministic candidates (12 were supplied by
      presegmented text), or fix the JSON parsing to handle the truncated
      response."
    ownedFiles:
      - server/src/compiler/pipeline/passes/ChatbotCompilePasses.ts
    acceptance:
      - ai_precompile pass no longer emits 'ai_precompile_shape_mismatch'
        warning for this protocol
      - outputs.ai_precompile.candidateEvents is non-empty (at least 4 events
        from the 8 protocol steps)
      - expand_biology_verbs pass is no longer skipped (when condition evaluates
        to true)
      - eventCount >= 3 in the final compiler output
    coderModelProfile:
      model: Kbenkhaled/Qwen3.5-35B-3A-NVFP4
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: &a1
      compiler: /home/brad/git/computable-foundry/artifacts/compiler/cellrox-oxidative-stress-qr/robot_deck.yaml
      eventGraph: /home/brad/git/computable-foundry/artifacts/event-graphs/cellrox-oxidative-stress-qr/robot_deck.yaml
      executionScale: /home/brad/git/computable-foundry/artifacts/execution-scale/cellrox-oxidative-stress-qr/robot_deck.yaml
      browserReport: /home/brad/git/computable-foundry/artifacts/browser-review/cellrox-oxidative-stress-qr/robot_deck/report.yaml
      coderPatch: /home/brad/git/computable-foundry/artifacts/code-patches/cellrox-oxidative-stress-qr/robot_deck/result.yaml
      assumptions: /home/brad/git/computable-foundry/artifacts/assumptions/cellrox-oxidative-stress-qr/robot_deck.yaml
      segment: /home/brad/git/computable-foundry/artifacts/segments/cellrox-oxidative-stress-qr.yaml
      materialContext: /home/brad/git/computable-foundry/artifacts/material-context/cellrox-oxidative-stress-qr.yaml
      text: /home/brad/git/computable-foundry/artifacts/text/cellrox-oxidative-stress-qr.txt
    failureEvidence: &a2
      eventCount: 0
      outcome: gap
    implementationBudget:
      targetChangedFiles: 1
      maxChangedFiles: 2
      targetChangedLines: 60
      maxChangedLines: 150
      requireFocusedFixture: true
  - id: fix-deterministic-candidate-to-event-lowering
    class: compiler_event_coverage
    title: Add deterministic lowering from presegmented candidates to
      candidateEvents when ai_precompile fails
    rationale: "The pre-compiler supplied 12 deterministic candidates from
      presegmented text, but the ai_precompile pass's fallback does not convert
      these candidates into candidateEvents. The PDF has clear steps: (1) treat
      cells with compound, (2) add CellROX Reagent at 5 μM, (3) incubate 30 min
      at 37°C, (4) remove medium and wash 3x with PBS, (5) optional fix with
      3.7% formaldehyde, (6) optional counterstain, (7) optional permeabilize
      with 0.5% Triton X-100, (8) analyze cells. The compiler should have a
      deterministic path that converts numbered protocol steps into
      candidateEvents with verbs like 'add_reagent', 'incubate', 'wash', 'fix',
      'stain', 'permeabilize', 'analyze'. The prior patch added an 'analyze'
      verb expander, but the events never reached the verb expander because
      candidateEvents was empty. This fix ensures that when ai_precompile fails,
      the deterministic candidates are still lowered to events."
    ownedFiles:
      - server/src/compiler/pipeline/passes/ChatbotCompilePasses.ts
      - server/src/compiler/pipeline/passes/DeterministicPrecompilePass.ts
    acceptance:
      - When ai_precompile produces empty candidateEvents, deterministic
        candidates are converted to at least 4 candidateEvents
      - Protocol steps with clear verbs (add, incubate, wash, fix, analyze) are
        recognized as candidateEvents
      - expand_biology_verbs pass processes these events and produces terminal
        events
      - Final eventCount >= 5 for this protocol
    coderModelProfile:
      model: Qwen/Qwen3.6-27B-FP8
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: *a1
    failureEvidence: *a2
    implementationBudget:
      targetChangedFiles: 2
      maxChangedFiles: 3
      targetChangedLines: 100
      maxChangedLines: 220
      requireFocusedFixture: true
sourceArtifacts: *a1
architectNotes: "LLM architect analysis: 2 fix recommendations."

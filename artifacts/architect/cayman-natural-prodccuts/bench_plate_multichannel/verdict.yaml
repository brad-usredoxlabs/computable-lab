kind: protocol-foundry-architect-verdict
protocolId: cayman-natural-prodccuts
variant: bench_plate_multichannel
generated_at: 2026-05-09T06:33:56.706Z
accepted: false
qualityScore: 0
coverageEstimate: 0
failureClasses:
  - precompiler_reference_shape
  - compiler_event_coverage
  - material_catalog_or_spec
missingVerbs: []
missingLabware: []
missingMaterials: []
badEvents: []
badScalingAssumptions: []
recommendedFixes:
  - id: precompiler-extract-plate-map-candidates
    class: precompiler_reference_shape
    title: Pre-compiler must extract plate-map candidates from Cayman 96-well
      library PDF
    rationale: "The pre-compiler segments contain no candidateEvents, entities, or
      labwareHints. The PDF clearly contains a structured plate map (Plate 1,
      wells A1-H12 with compound names and item numbers). The pre-compiler's
      extract_entities pass is producing zero usable candidates for the compiler
      to lower into events. This is a pre-compiler extraction failure: the
      prompt or regex patterns do not recognize the Cayman Chemical plate-map
      table format."
    ownedFiles:
      - server/src/precompiler/extractors/plate-map-extractor.ts
      - server/src/precompiler/prompts/extract-entities.ts
    acceptance:
      - Pre-compiler output for cayman-natural-prodccuts must contain at least 1
        candidateEvent with kind 'plate_map' or 'load_plate' containing
        well-to-compound mappings.
      - Pre-compiler must extract at least 20 compound entities with name and
        item_number from the plate map table.
      - Pre-compiler must emit a labwareHint for a 96-well plate (e.g.,
        '96_well_plate' or 'plate_96').
      - Compiler eventCount must be >= 1 after this fix (at minimum a
        plate-loading event).
    coderModelProfile:
      model: Qwen/Qwen3.6-27B-FP8
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: &a1
      compiler: /home/brad/git/computable-foundry/artifacts/compiler/cayman-natural-prodccuts/bench_plate_multichannel.yaml
      eventGraph: /home/brad/git/computable-foundry/artifacts/event-graphs/cayman-natural-prodccuts/bench_plate_multichannel.yaml
      executionScale: /home/brad/git/computable-foundry/artifacts/execution-scale/cayman-natural-prodccuts/bench_plate_multichannel.yaml
      browserReport: /home/brad/git/computable-foundry/artifacts/browser-review/cayman-natural-prodccuts/bench_plate_multichannel/report.yaml
      coderPatch: /home/brad/git/computable-foundry/artifacts/code-patches/cayman-natural-prodccuts/bench_plate_multichannel/result.yaml
      assumptions: /home/brad/git/computable-foundry/artifacts/assumptions/cayman-natural-prodccuts/bench_plate_multichannel.yaml
      segment: /home/brad/git/computable-foundry/artifacts/segments/cayman-natural-prodccuts.yaml
      materialContext: /home/brad/git/computable-foundry/artifacts/material-context/cayman-natural-prodccuts.yaml
      text: /home/brad/git/computable-foundry/artifacts/text/cayman-natural-prodccuts.txt
    failureEvidence: &a2
      eventCount: 0
      outcome: gap
    implementationBudget:
      targetChangedFiles: 2
      maxChangedFiles: 3
      targetChangedLines: 120
      maxChangedLines: 250
      requireFocusedFixture: true
  - id: compiler-lower-plate-map-to-events
    class: compiler_event_coverage
    title: Compiler must lower plate-map candidates to load_plate events
    rationale: Even if the pre-compiler produces plate-map candidates, the compiler
      currently produces 0 events. The compiler lacks a lowering rule for
      plate-map / load_plate candidate types. For a screening library protocol,
      the primary event is loading compounds into a 96-well plate. The compiler
      needs a verb handler that transforms a plate-map candidate (well ->
      compound mapping) into a load_plate event with well assignments.
    ownedFiles:
      - server/src/compiler/lowering/plate-map-lowering.ts
      - server/src/compiler/verbs/load-plate.ts
      - server/src/compiler/event-graph-builder.ts
    acceptance:
      - Compiler must produce at least 1 event of kind 'load_plate' or
        'assign_wells' when given a plate-map candidate.
      - The event graph must contain well-to-compound assignments matching the
        PDF plate map (e.g., A2 -> Ursolic Acid, A3 -> (+)-Abscisic Acid).
      - eventCount must be >= 1 (ideally >= 2 including a plate initialization
        event).
      - outcome must not be 'gap' after this fix.
    coderModelProfile:
      model: Qwen/Qwen3.6-27B-FP8
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: *a1
    failureEvidence: *a2
    implementationBudget:
      targetChangedFiles: 2
      maxChangedFiles: 4
      targetChangedLines: 100
      maxChangedLines: 200
      requireFocusedFixture: true
  - id: material-catalog-cayman-compounds
    class: material_catalog_or_spec
    title: Material context must resolve Cayman compound names to material records
    rationale: "The material context is empty (materials: [], materialSpecs: [],
      vendorProducts: []). The PDF lists 40+ compounds with Cayman item numbers
      (e.g., Ursolic Acid 10072, Paclitaxel 10461). Without material records,
      the compiler cannot resolve compound references, and the execution scale
      cannot bind reagents. The material extractor must parse the plate map
      table and create vendor-product and material-spec records for each
      compound."
    ownedFiles:
      - server/src/precompiler/materials/cayman-material-extractor.ts
      - server/src/materials/vendor-product-resolver.ts
    acceptance:
      - Material context must contain at least 20 material records corresponding
        to compounds in the plate map.
      - Each material record must include name, vendor (Cayman Chemical), and
        item_number.
      - Compiler must be able to resolve compound names in plate-map candidates
        to material IDs.
      - Execution scale reagentLayout must reference resolved material roles
        instead of provisional placeholders.
    coderModelProfile:
      model: Qwen/Qwen3.6-35B-A3B-FP8
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: *a1
    failureEvidence: *a2
    implementationBudget:
      targetChangedFiles: 2
      maxChangedFiles: 3
      targetChangedLines: 80
      maxChangedLines: 180
      requireFocusedFixture: true
sourceArtifacts: *a1
architectNotes: "LLM architect analysis: 3 fix recommendations."

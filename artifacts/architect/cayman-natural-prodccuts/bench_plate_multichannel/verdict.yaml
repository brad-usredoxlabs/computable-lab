kind: protocol-foundry-architect-verdict
protocolId: cayman-natural-prodccuts
variant: bench_plate_multichannel
generated_at: 2026-05-09T22:14:48.927Z
accepted: false
qualityScore: 0.05
coverageEstimate: 0
failureClasses:
  - compiler_event_coverage
  - precompiler_reference_shape
missingVerbs: []
missingLabware: []
missingMaterials: []
badEvents: []
badScalingAssumptions: []
recommendedFixes:
  - id: add-plate-map-verb-lowering
    class: compiler_event_coverage
    title: Add plate-map verb lowering for 96-well screening library protocols
    rationale: The PDF is a Cayman Chemical Natural Products Screening Library plate
      map (96-well). The pre-compiler extracted 23+ candidate segments
      (deterministic candidates from presegmented text), but the compiler
      produced 0 events. The compiler lacks a verb/action mapping for
      'plate_map' or 'screening_library' content types. The prior patch touched
      BiologyVerbExpander.ts but the event graph is still empty, meaning the
      verb lowering path was not triggered or the candidates were not shaped
      correctly for the compiler to consume. The compiler needs a dedicated
      lowering pass that recognizes plate-map-style candidates (well-position +
      compound-name + catalog-number) and emits 'load_plate' or 'map_compounds'
      events.
    ownedFiles:
      - server/src/compiler/biology/BiologyVerbExpander.ts
      - server/src/compiler/pipeline/passes/ChatbotCompilePasses.ts
    acceptance:
      - Compiler produces >= 1 event for a plate-map protocol with
        well-position/compound/catalog-number candidates
      - Event graph contains events with verb 'load_plate' or 'map_compounds'
        referencing the 96-well plate labware
      - Each compound from the plate map (e.g., Ursolic Acid, Paclitaxel) is
        represented as a material reference in at least one event
      - eventCount >= 3 for this protocol variant
    coderModelProfile:
      model: Qwen/Qwen3.6-27B-FP8
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: &a1
      compiler: /home/brad/git/computable-foundry/artifacts/compiler/cayman-natural-prodccuts/bench_plate_multichannel.yaml
      eventGraph: /home/brad/git/computable-foundry/artifacts/event-graphs/cayman-natural-prodccuts/bench_plate_multichannel.yaml
      executionScale: /home/brad/git/computable-foundry/artifacts/execution-scale/cayman-natural-prodccuts/bench_plate_multichannel.yaml
      browserReport: /home/brad/git/computable-foundry/artifacts/browser-review/cayman-natural-prodccuts/bench_plate_multichannel/report.yaml
      coderPatch: /home/brad/git/computable-foundry/artifacts/code-patches/cayman-natural-prodccuts/bench_plate_multichannel/result.yaml
      assumptions: /home/brad/git/computable-foundry/artifacts/assumptions/cayman-natural-prodccuts/bench_plate_multichannel.yaml
      segment: /home/brad/git/computable-foundry/artifacts/segments/cayman-natural-prodccuts.yaml
      materialContext: /home/brad/git/computable-foundry/artifacts/material-context/cayman-natural-prodccuts.yaml
      text: /home/brad/git/computable-foundry/artifacts/text/cayman-natural-prodccuts.txt
    failureEvidence: &a2
      eventCount: 0
      outcome: gap
    implementationBudget:
      targetChangedFiles: 2
      maxChangedFiles: 3
      targetChangedLines: 120
      maxChangedLines: 220
      requireFocusedFixture: true
  - id: fix-precompiler-plate-map-candidate-shape
    class: precompiler_reference_shape
    title: Ensure pre-compiler emits structured plate-map candidates with well
      positions and compound names
    rationale: "The diagnostics show 'foundry_presegmented_candidates' messages but
      the candidates are not being converted to events. The pre-compiler likely
      extracts raw text segments without structuring them into the shape the
      compiler expects (e.g., {well: 'A2', compound: 'Ursolic Acid',
      catalogNumber: '10072'}). The compiler's extract_entities pass sees
      candidates but cannot match them to any known verb because the candidate
      shape doesn't include the semantic fields needed for plate-map lowering.
      The pre-compiler (DeterministicPrecompilePass.ts or the extraction
      service) needs to parse the tabular plate map format and emit structured
      candidates with well positions, compound names, and catalog numbers."
    ownedFiles:
      - server/src/compiler/pipeline/passes/DeterministicPrecompilePass.ts
      - server/src/extract/ExtractionRunnerService.ts
    acceptance:
      - "Pre-compiler output includes candidates with fields: well (e.g., 'A2'),
        compound (e.g., 'Ursolic Acid'), catalogNumber (e.g., '10072')"
      - Candidates are not just raw text segments but structured objects the
        compiler can match to plate-map verbs
      - Compiler diagnostics no longer show 'foundry_presegmented_candidates' as
        the only signal without subsequent event generation
      - At least 20 structured candidates are emitted for the Cayman plate map
        (matching the visible wells in the PDF)
    coderModelProfile:
      model: Qwen/Qwen3.6-27B-FP8
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: *a1
    failureEvidence: *a2
    implementationBudget:
      targetChangedFiles: 2
      maxChangedFiles: 3
      targetChangedLines: 100
      maxChangedLines: 200
      requireFocusedFixture: true
sourceArtifacts: *a1
architectNotes: "LLM architect analysis: 2 fix recommendations."

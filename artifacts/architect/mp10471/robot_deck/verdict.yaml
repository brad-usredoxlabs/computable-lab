kind: protocol-foundry-architect-verdict
protocolId: mp10471
variant: robot_deck
generated_at: 2026-05-09T22:43:30.708Z
accepted: false
qualityScore: 0.05
coverageEstimate: 0
failureClasses:
  - precompiler_reference_shape
  - extractor_prompt_contract
missingVerbs: []
missingLabware: []
missingMaterials: []
badEvents: []
badScalingAssumptions: []
recommendedFixes:
  - id: fix-ai-precompile-shape-mismatch-and-fallback
    class: precompiler_reference_shape
    title: Fix ai_precompile shape mismatch fallback to preserve candidateEvents
    rationale: The compiler diagnostic 'ai_precompile_shape_mismatch' indicates the
      LLM's precompile output did not match the expected AiPrecompileOutput
      schema. The current fallback produces an empty output (no candidateEvents,
      no candidateLabwares), causing all downstream passes
      (expand_biology_verbs, resolve_labware, mint_materials) to be skipped.
      This results in eventCount=0 despite the PDF containing clear protocol
      steps. The fallback should preserve any partially parsed data or use a
      deterministic extraction path instead of returning empty arrays.
    ownedFiles:
      - server/src/compiler/pipeline/passes/ChatbotCompilePasses.ts
    acceptance:
      - When ai_precompile output has shape mismatch, the fallback must not
        produce empty candidateEvents if deterministic candidates were extracted
      - The fallback should merge deterministic presegmented candidates into
        candidateEvents when LLM output is malformed
      - eventCount must be >= 3 for protocols with clear procedural steps in PDF
        text
      - Downstream passes (expand_biology_verbs, resolve_labware) must not be
        skipped when candidates exist
    coderModelProfile:
      model: Kbenkhaled/Qwen3.5-35B-3A-NVFP4
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: &a1
      compiler: /home/brad/git/computable-foundry/artifacts/compiler/mp10471/robot_deck.yaml
      eventGraph: /home/brad/git/computable-foundry/artifacts/event-graphs/mp10471/robot_deck.yaml
      executionScale: /home/brad/git/computable-foundry/artifacts/execution-scale/mp10471/robot_deck.yaml
      browserReport: /home/brad/git/computable-foundry/artifacts/browser-review/mp10471/robot_deck/report.yaml
      coderPatch: /home/brad/git/computable-foundry/artifacts/code-patches/mp10471/robot_deck/result.yaml
      assumptions: /home/brad/git/computable-foundry/artifacts/assumptions/mp10471/robot_deck.yaml
      segment: /home/brad/git/computable-foundry/artifacts/segments/mp10471.yaml
      materialContext: /home/brad/git/computable-foundry/artifacts/material-context/mp10471.yaml
      text: /home/brad/git/computable-foundry/artifacts/text/mp10471.txt
    failureEvidence: &a2
      eventCount: 0
      outcome: gap
    implementationBudget:
      targetChangedFiles: 1
      maxChangedFiles: 2
      targetChangedLines: 60
      maxChangedLines: 150
      requireFocusedFixture: true
  - id: fix-tag-prompt-json-parse-error
    class: extractor_prompt_contract
    title: Fix tag_prompt JSON parse error from unterminated strings
    rationale: The tag_prompt pass fails with 'Unterminated string in JSON at
      position 594', indicating the LLM response contains malformed JSON (likely
      unescaped quotes or special characters in material names like 'Fluo-4
      Direct™'). This parse failure contributes to the shape mismatch in
      ai_precompile. The prompt contract should enforce stricter JSON output or
      the parser should handle common LLM JSON errors gracefully.
    ownedFiles:
      - server/src/compiler/pipeline/passes/ChatbotCompilePasses.ts
      - server/src/extract/ExtractionRunnerService.ts
    acceptance:
      - tag_prompt pass must not fail on valid JSON with special characters in
        values
      - Parser should handle or reject malformed JSON gracefully without
        crashing the pipeline
      - Material names with special characters (™, °, etc.) should be properly
        escaped in JSON output
    coderModelProfile:
      model: Kbenkhaled/Qwen3.5-35B-3A-NVFP4
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: *a1
    failureEvidence: *a2
    implementationBudget:
      targetChangedFiles: 1
      maxChangedFiles: 2
      targetChangedLines: 40
      maxChangedLines: 100
      requireFocusedFixture: true
  - id: fix-deterministic-candidate-to-event-lowering
    class: compiler_event_coverage
    title: Lower deterministic presegmented candidates to events when LLM precompile
      fails
    rationale: The diagnostics show 8, 10, 24, 25, 1, and 4 deterministic candidates
      were extracted from presegmented text, but these are never converted to
      events because the ai_precompile pass fails and returns empty
      candidateEvents. The compiler should have a fallback path that converts
      deterministic candidates (which represent protocol steps) into basic
      events even when the LLM precompile fails. This ensures protocols with
      clear procedural content produce at least some events.
    ownedFiles:
      - server/src/compiler/pipeline/passes/ChatbotCompilePasses.ts
      - server/src/compiler/biology/BiologyVerbExpander.ts
    acceptance:
      - When ai_precompile fails, deterministic candidates should be lowered to
        basic events
      - Event graph should contain at least 3 events for protocols with clear
        procedural steps
      - Events should reference extracted labware and materials from
        deterministic candidates
      - The expand_biology_verbs pass should process deterministic candidates
        when LLM candidates are empty
    coderModelProfile:
      model: Qwen/Qwen3.6-27B-FP8
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: *a1
    failureEvidence: *a2
    implementationBudget:
      targetChangedFiles: 2
      maxChangedFiles: 3
      targetChangedLines: 100
      maxChangedLines: 220
      requireFocusedFixture: true
sourceArtifacts: *a1
architectNotes: "LLM architect analysis: 3 fix recommendations."

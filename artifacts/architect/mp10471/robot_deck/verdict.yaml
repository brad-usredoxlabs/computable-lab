kind: protocol-foundry-architect-verdict
protocolId: mp10471
variant: robot_deck
generated_at: 2026-05-09T09:40:13.279Z
accepted: false
qualityScore: 0.1
coverageEstimate: 0.1
failureClasses:
  - precompiler_reference_shape
  - compiler_event_coverage
missingVerbs: []
missingLabware: []
missingMaterials: []
badEvents: []
badScalingAssumptions: []
recommendedFixes:
  - id: fix-ai-precompile-shape-mismatch
    class: precompiler_reference_shape
    title: Fix ai_precompile output shape mismatch to enable downstream passes
    rationale: The compiler diagnostics show 'ai_precompile output shape mismatch',
      which causes all downstream passes (mint_materials, apply_directives,
      resolve_labware, expand_patterns) to be skipped because their 'when'
      conditions evaluate to false. This is the root cause of the gap outcome.
      The ai_precompile pass in ChatbotCompilePasses.ts is producing output that
      doesn't match the expected AiPrecompileOutput type schema, preventing the
      compiler from extracting candidates, labwares, and directives needed to
      generate meaningful events.
    ownedFiles:
      - server/src/compiler/pipeline/passes/ChatbotCompilePasses.ts
    acceptance:
      - ai_precompile pass produces output matching AiPrecompileOutput type
        schema
      - No 'ai_precompile_shape_mismatch' warning in diagnostics
      - Downstream passes (mint_materials, resolve_labware) are no longer
        skipped due to false when conditions
      - candidateLabwares and directives fields are populated when present in
        LLM output
    coderModelProfile:
      model: Qwen/Qwen3.6-27B-FP8
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: &a1
      compiler: /home/brad/git/computable-foundry/artifacts/compiler/mp10471/robot_deck.yaml
      eventGraph: /home/brad/git/computable-foundry/artifacts/event-graphs/mp10471/robot_deck.yaml
      executionScale: /home/brad/git/computable-foundry/artifacts/execution-scale/mp10471/robot_deck.yaml
      browserReport: /home/brad/git/computable-foundry/artifacts/browser-review/mp10471/robot_deck/report.yaml
      coderPatch: /home/brad/git/computable-foundry/artifacts/code-patches/mp10471/robot_deck/result.yaml
      assumptions: /home/brad/git/computable-foundry/artifacts/assumptions/mp10471/robot_deck.yaml
      segment: /home/brad/git/computable-foundry/artifacts/segments/mp10471.yaml
      materialContext: /home/brad/git/computable-foundry/artifacts/material-context/mp10471.yaml
      text: /home/brad/git/computable-foundry/artifacts/text/mp10471.txt
    failureEvidence: &a2
      eventCount: 2
      outcome: gap
    implementationBudget:
      targetChangedFiles: 1
      maxChangedFiles: 2
      targetChangedLines: 60
      maxChangedLines: 150
      requireFocusedFixture: true
  - id: fix-verb-expansion-for-assay-protocol
    class: compiler_event_coverage
    title: Add verb lowering for Fluo-4 assay preparation and loading steps
    rationale: The PDF describes a calcium assay protocol with clear steps (prepare
      loading solution, add to cells, incubate, measure fluorescence), but the
      compiler only produces 2 empty mix events. The BiologyVerbExpander needs
      to recognize assay preparation verbs like 'prepare loading solution', 'add
      reagent to cells', 'incubate', and 'measure fluorescence' and lower them
      to appropriate liquid handling events (dispense, mix, incubate, read).
      Without these verb mappings, the compiler cannot generate meaningful
      events from the extracted candidates.
    ownedFiles:
      - server/src/compiler/biology/BiologyVerbExpander.ts
    acceptance:
      - Verb expander recognizes 'prepare loading solution' and lowers to
        dispense/mix events
      - Verb expander recognizes 'add reagent to cells' and lowers to dispense
        events
      - Verb expander recognizes 'incubate' and lowers to incubate events
      - Event count increases to at least 5 events for a complete assay protocol
      - Events include proper labware references (sample_plate,
        reagent_reservoir)
    coderModelProfile:
      model: Qwen/Qwen3.6-27B-FP8
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: *a1
    failureEvidence: *a2
    implementationBudget:
      targetChangedFiles: 1
      maxChangedFiles: 2
      targetChangedLines: 80
      maxChangedLines: 200
      requireFocusedFixture: true
sourceArtifacts: *a1
architectNotes: "LLM architect analysis: 2 fix recommendations."

kind: protocol-foundry-architect-verdict
protocolId: mp10471
variant: bench_plate_multichannel
generated_at: 2026-05-09T22:43:05.617Z
accepted: false
qualityScore: 0
coverageEstimate: 0
failureClasses:
  - precompiler_reference_shape
  - extractor_prompt_contract
missingVerbs: []
missingLabware: []
missingMaterials: []
badEvents: []
badScalingAssumptions: []
recommendedFixes:
  - id: fix-ai-precompile-shape-mismatch-and-fallback
    class: precompiler_reference_shape
    title: Fix ai_precompile shape mismatch and ensure candidateEvents are populated
      from deterministic candidates
    rationale: The compiler output shows 'ai_precompile_shape_mismatch' warning,
      causing the fallback path to be used. This results in empty
      candidateEvents, which causes all downstream passes (expand_biology_verbs,
      resolve_labware, etc.) to be skipped via 'when' conditions. The
      pre-compiler extracted 70+ deterministic candidates (7+10+24+25+1+4), but
      these are not being converted into candidateEvents by the ai_precompile
      pass. The tag_prompt also had a JSON parse error ('Unterminated string'),
      suggesting the LLM output is malformed. The root cause is that the
      ai_precompile pass in ChatbotCompilePasses.ts is not correctly shaping its
      output or handling the deterministic candidates from the presegmented
      extraction.
    ownedFiles:
      - server/src/compiler/pipeline/passes/ChatbotCompilePasses.ts
    acceptance:
      - ai_precompile pass produces valid AiPrecompileOutput with non-empty
        candidateEvents when deterministic candidates exist
      - No 'ai_precompile_shape_mismatch' warnings in diagnostics when
        candidates are present
      - Downstream passes (expand_biology_verbs, resolve_labware) are not
        skipped when candidateEvents exist
      - eventCount >= 3 for protocols with clear procedural steps in PDF text
    coderModelProfile:
      model: Qwen/Qwen3.6-27B-FP8
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: &a1
      compiler: /home/brad/git/computable-foundry/artifacts/compiler/mp10471/bench_plate_multichannel.yaml
      eventGraph: /home/brad/git/computable-foundry/artifacts/event-graphs/mp10471/bench_plate_multichannel.yaml
      executionScale: /home/brad/git/computable-foundry/artifacts/execution-scale/mp10471/bench_plate_multichannel.yaml
      browserReport: /home/brad/git/computable-foundry/artifacts/browser-review/mp10471/bench_plate_multichannel/report.yaml
      coderPatch: /home/brad/git/computable-foundry/artifacts/code-patches/mp10471/bench_plate_multichannel/result.yaml
      assumptions: /home/brad/git/computable-foundry/artifacts/assumptions/mp10471/bench_plate_multichannel.yaml
      segment: /home/brad/git/computable-foundry/artifacts/segments/mp10471.yaml
      materialContext: /home/brad/git/computable-foundry/artifacts/material-context/mp10471.yaml
      text: /home/brad/git/computable-foundry/artifacts/text/mp10471.txt
    failureEvidence: &a2
      eventCount: 0
      outcome: gap
    implementationBudget:
      targetChangedFiles: 1
      maxChangedFiles: 2
      targetChangedLines: 100
      maxChangedLines: 250
      requireFocusedFixture: true
  - id: fix-tag-prompt-json-parse-error
    class: extractor_prompt_contract
    title: Fix tag_prompt JSON parsing to handle truncated or malformed LLM responses
    rationale: The tag_prompt pass failed with 'Unterminated string in JSON at
      position 705', indicating the LLM response was truncated or malformed.
      This prevents proper entity tagging (materials, labware, etc.) which feeds
      into the ai_precompile pass. The fix should add robust JSON parsing with
      fallback recovery (e.g., attempting to parse partial JSON, or using a more
      lenient parser) and ensure the prompt contract enforces complete JSON
      output.
    ownedFiles:
      - server/src/compiler/pipeline/passes/ChatbotCompilePasses.ts
    acceptance:
      - tag_prompt pass does not fail on truncated JSON responses
      - Partial JSON responses are recovered or gracefully degraded
      - Entity tagging (materials, labware) proceeds even with imperfect LLM
        output
      - No 'tag_prompt_parse_error' warnings in diagnostics
    coderModelProfile:
      model: Kbenkhaled/Qwen3.5-35B-3A-NVFP4
      guidance: Narrow ownership, concrete evidence, one behavior change.
    sourceArtifacts: *a1
    failureEvidence: *a2
    implementationBudget:
      targetChangedFiles: 1
      maxChangedFiles: 2
      targetChangedLines: 60
      maxChangedLines: 150
      requireFocusedFixture: true
sourceArtifacts: *a1
architectNotes: "LLM architect analysis: 2 fix recommendations."

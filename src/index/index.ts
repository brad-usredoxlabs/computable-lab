/**
 * Record Index module.
 * 
 * Provides fast querying and tree navigation for records
 * without parsing all YAML files on every request.
 */

export * from './types.js';
export * from './IndexManager.js';

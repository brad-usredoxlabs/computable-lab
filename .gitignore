test-lab/
git/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build output
dist/
build/
tmp/

# Runtime
.run/
.ralph/
.fixit-worktrees/
.resume
server/var/
artifacts/event-editor-fixit/jobs/

# Generated by vite-plugin-pwa during dev/build
app/dev-dist/

# Environment and config
.env
config.yaml

# Test artifacts
test-results/
screenshots/
playwright-report/

# Symlinks in server/ (point to monorepo root)
server/schema
server/records
server/config

# Ontology download cache
scripts/.ontology-cache/*
!scripts/.ontology-cache/.gitkeep

# Playwright MCP runtime artifacts
.playwright-mcp/

# AI overlay bundles served by vite preview at runtime — populated by
# ansible (cla-lab-ai-overlay role) or symlinked from cl-appliance for
# local dev. Never committed; the source of truth lives in cl-appliance.
app/public/overlays/

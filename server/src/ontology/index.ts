/**
 * Ontology module exports for computable-lab.
 * 
 * Provides:
 * - Domain mappings (which ontology for which semantic domain)
 * - Picker configurations (for UI components)
 */

export * from './domains.js'
export * from './pickerConfig.js'
